# Pinned versions used for the reported benchmark results.
# Light env (electr-forecast): runs SeasonalNaive / SARIMA / XGBoost
# experiments and post-processing (tables, figures, statistical tests).
numpy==2.4.3
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.8.0
matplotlib==3.10.9
seaborn==0.13.2
plotly==6.7.0
kaleido==0.2.1
pyyaml==6.0.3
tqdm==4.67.3
xgboost==3.2.0
statsforecast==2.0.3

# Foundation env (electr-forecast-foundation): runs neural and zero-shot
# foundation models. Upper bounds are loose because some of these track
# rapidly-moving upstreams; record exact versions used at run time in logs.
torch>=2.2
neuralforecast>=1.7
chronos-forecasting>=1.5
autogluon.timeseries>=1.2
timesfm
